---

- name: Configure the server and install required software
  hosts: localhost
  gather_facts: false
  become: true
  vars_files:
    - vars/config.yml

  roles:
    - role: pre
      tags: always
    - role: dns_adblocking
      tags:
        - dns
        - adblock
    - role: ssh_tunneling
      tags: ssh_tunneling
    - role: security
      tags: security
    - role: vpn
      tags: vpn
