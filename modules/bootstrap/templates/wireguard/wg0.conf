[Interface]
Address = ${Address.ipv4} %{ if ipv6 ~}, ${Address.ipv6}%{ endif }
ListenPort = ${ListenPort}
PrivateKey = ${PrivateKey}
SaveConfig = false

%{ for k in Peers ~}

[Peer]
# ${k.User}
PublicKey = ${k.PublicKey}
AllowedIPs = ${k.AllowedIPs.ipv4} %{ if ipv6 ~}, ${k.AllowedIPs.ipv6}%{ endif }
PresharedKey = ${k.PresharedKey}

%{ endfor ~}
